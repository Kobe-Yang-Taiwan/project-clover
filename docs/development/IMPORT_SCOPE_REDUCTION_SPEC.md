# Import scope reduction — active execution contract

> CURRENT AUTHORITY — 2026-09-07: Founder authorized EXECUTE MODE for
> **0.17.0+20 Simplified Playable MVP**. **REDUCE IMPORT AMBITION**.
> Problem: benefits are forgotten, expire, and lose value. Outcome:
> **Benefit Captured / Money Saved**. Current MVP: image Capture → Name /
> Expiration / Value Draft → Confirm → local Save → Reminder.
> Full Product Reconstruction and diagnostic root-cause completion are not
> blocking requirements. Older no-Build-20 / diagnostic-first gates below are
> historical and superseded. Existing negative cases remain unresolved.
> Status: IMPLEMENTING; new QA/APK pending; Founder Test PENDING.
> Next authorized task: finish targeted implementation, tests and Android build.
> Current specification: docs/development/SIMPLIFIED_MVP_BUILD20.md.
> Results: docs/development/BUILD20_EVIDENCE.md. No main merge authorized.

Founder approval to resume: 2026-09-05. Supersedes older V0.16R reconstruction
mission/KPI/acceptance direction, not its privacy or evidence protections.

Goal: Minimum Change. Maximum Evidence. Full Product Reconstruction is not an MVP
Gate. Future target: Critical Draft -> Confidence -> Quick Confirmation -> Save
-> Reminder, using Name / Expiration / Value. See `../context/DECISIONS.md`.

## Current allowed scope

Phase 0: create six missing context files from verified records and Founder input;
update current spec/dashboard. Explicit resumption approval resolves the previous
missing-document stop. Unknown history and measurements must stay unknown.

Phase 1: diagnostic logging/data structures, tests, reproducible fixtures and trace
tooling only. Before any code edit state the smallest behavior-preserving change.
No threshold, price anchor, region proposal, grouping, ownership, reconstruction,
classification, UX, cloud/provider or persistence behavior changes.

PX trace: input hash/version, full OCR raw/normalized text and stable line IDs,
bounds and optional confidence, semantic types, accepted/rejected anchor IDs and
machine-readable actual rule reasons, region IDs/bounds/source anchors/included
line IDs/grouping reason, ownership, crop OCR, reconstruction, candidate/excluded
IDs/types/reasons/mapping. Record entering/leaving counts at every stage and first
proven rule-level divergence. Unknown confidence remains null.

PDF Page 2 trace: document retailer, page routing, context passed into cell builder,
selected builder, ITEM count, expected 16/actual cell count, non-ITEM products,
SEALY/MICHELIN expected versus actual ownership, result merchantHint -> parser
merchant. Categorize all 229 reported excluded records only from an actual export;
unknown bucket counts are not zero.

Required tests: actual PX 12->7 failing replay, stable trace, PDF Page 2 routing/
ownership/context baseline, diagnostic-enabled versus disabled output equivalence,
targeted tests and full regression. Authored coordinate tests are not real recall.
Keep raw private fixtures and diagnostic exports off public git/cloud. No automatic
logging/upload of personal input. Record missing environment/device capabilities.

## Stop and handoff

STOP-1 unknown first rule cause; STOP-2 behavior/scope expansion; STOP-3 privacy,
security, data loss, auth, payment or upload risk; STOP-4 missing reproducible input,
trace or validation capability. Do not guess. Deliver evidence gaps and minimum
recovery request. No Build 20, APK/CI build trigger, Phase 2 or main merge.
If device capture needs a new diagnostic APK, obtain explicit approval for its
build/version and private export workflow first. Do not publish raw sources.

Output: traces, first causes/confidence, evidence, exclusions breakdown, failing
fixtures, tests/results, changed files/diff, failures/unknowns/blast radius; choose
ONE: A minimal fix, B critical draft POC, C architecture review, D insufficient
evidence. Stop for independent evidence review/Founder decision. No automatic fix.

## Future, not authorized

Tap-first dense PDF and three-field POC; protocol in `../context/BUSINESS_VALIDATION.md`.
No iOS, login, sync, new formats, recommendations, monetization, marketplace,
new AI infrastructure, value dashboard or parser rewrite.
